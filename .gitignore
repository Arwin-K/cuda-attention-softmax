# Python
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# Native-extension build products
build/
dist/
*.egg-info/
*.so
*.dylib

# Measured CSVs and publication figures are intentionally not ignored. Their
# Git commit, environment metadata, and review diff are part of the research
# provenance. Large profiler traces remain local unless a later planned commit
# explicitly chooses a compact artifact.

# Local tooling
.DS_Store
.ipynb_checkpoints/
